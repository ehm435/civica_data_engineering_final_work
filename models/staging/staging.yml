version: 2

models:
  - name: base_maps_bangkok_2025
    description: "Clean base model for maps_bangkok. Adds a fixed event_id and applies minimal column cleaning."
    config:
      tags: ["base", "maps", "bangkok"]
    columns:
      - name: event_id
        description: "Logical identifier for the event (e.g., masters_bangkok_2025)"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_bangkok_2025"]
      - name: map_id
        description: "Map identifier local to this specific tournament"
        tests:
          - not_null
      - name: map_name
        description: "Name of the map as provided in the raw data"
        tests:
          - not_null
      - name: last_rework
        description: "Timestamp or text indicating when the map was last reworked"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "map_id"]

  - name: base_maps_paris_2025
    description: "Clean base model for maps_paris. Adds a fixed event_id and applies minimal column cleaning."
    config:
      tags: ["base", "maps", "paris"]
    columns:
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_paris_2025"]
      - name: map_id
        description: "Map identifier local to this specific tournament"
        tests:
          - not_null
      - name: map_name
        description: "Name of the map as provided in the raw data"
        tests:
          - not_null
      - name: last_rework
        description: "Timestamp or text indicating when the map was last reworked"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "map_id"]

  - name: base_maps_toronto_2025
    description: "Clean base model for maps_toronto. Adds a fixed event_id and applies minimal column cleaning."
    config:
      tags: ["base", "maps", "toronto"]
    columns:
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_toronto_2025"]
      - name: map_id
        description: "Map identifier local to this specific tournament"
        tests:
          - not_null
      - name: map_name
        description: "Name of the map as provided in the raw data"
        tests:
          - not_null
      - name: last_rework
        description: "Timestamp or text indicating when the map was last reworked"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "map_id"]

  - name: base_teams_bangkok_2025
    description: "Clean base model for teams_bangkok. Adds a fixed event_id and applies minimal column cleaning."
    config:
      tags: ["base", "maps", "bangkok"]
    columns:
      - name: event_id
        description: "Logical identifier for the event (e.g., masters_bangkok_2025)"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_bangkok_2025"]
      - name: team_id
        description: "team identifier local to this specific tournament"
        tests:
          - not_null
      - name: team_name
        description: "Name of the team as provided in the raw data"
        tests:
          - not_null
      - name: roster_made
        description: "Timestamp of last member changed"
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "team_id"]

  - name: base_teams_paris_2025
    description: "Clean base model for teams_paris. Adds a fixed event_id and applies minimal column cleaning."
    config:
      tags: ["base", "teams", "paris"]
    columns:
      - name: event_id
        description: "Logical identifier for the event (e.g., masters_paris_2025)"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_paris_2025"]
      - name: team_id
        description: "team identifier local to this specific tournament"
        tests:
          - not_null
      - name: team_name
        description: "Name of the team as provided in the raw data"
        tests:
          - not_null
      - name: roster_made
        description: "Timestamp of last member changed"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "team_id"]

  - name: base_teams_toronto_2025
    description: "Clean base model for teams_toronto. Adds a fixed event_id and applies minimal column cleaning."
    config:
      tags: ["base", "teams", "toronto"]
    columns:
      - name: event_id
        description: "Logical identifier for the event (e.g., masters_toronto_2025)"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_toronto_2025"]
      - name: team_id
        description: "team identifier local to this specific tournament"
        tests:
          - not_null
      - name: team_name
        description: "Name of the team as provided in the raw data"
        tests:
          - not_null
      - name: roster_made
        description: "Timestamp of last member changed"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "team_id"]

  - name: stg_maps
    description: >
      Bridge table that maps each (event_id, maps_name) pair — which uniquely
      identifies a map inside a single tournament — to a global surrogate key (map_pk)
      computed from the normalized map name.
    config:
      tags: ["staging", "bridge", "maps"]
    columns:
      - name: map_pk
        description: "Global surrogate key for the map"
        tests:
          - not_null
      - name: event_id
        description: "Event to which this map_id belongs"
        tests:
          - not_null
      - name: map_id
        description: "Map identifier inside the event (local key)"
        tests:
          - not_null
      - name: map_name
        description: "Raw map name used to compute the normalized identifier"
        tests:
          - not_null
      - name: last_rework
        description: "Last known map rework information"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "map_name"]
      - dbt_utils.unique:
          arguments:
            column_name: map_pk

  - name: stg_teams
    description: >
      Bridge table that maps each (event_id, team_name) pair — which uniquely
      identifies a team inside a single tournament — to a global surrogate key (team_pk)
      computed from the normalized team name.
    config:
      tags: ["staging", "bridge", "teams"]
    columns:
      - name: team_pk
        description: "Global surrogate key for the team"
        tests:
          - not_null
      - name: event_id
        description: "Event to which this team_id belongs"
        tests:
          - not_null
      - name: team_id
        description: "team identifier inside the event (local key)"
        tests:
          - not_null
      - name: team_name
        description: "Raw team name used to compute the normalized identifier"
        tests:
          - not_null

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "team_name"]
      - dbt_utils.unique:
          arguments:
            column_name: team_pk

  - name: base_matches_bangkok_2025
    description: "Clean base model for matches_bangkok. Adds a fixed event_id and applies minimal column cleaning."
    config:
      tags: ["base", "matches", "bangkok"]
    columns:
      - name: match_id
        description: "Match identifier local to this specific tournament"
        tests:
          - not_null
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_bangkok_2025"]
      - name: match_title
        description: "Raw match title"
        tests:
          - not_null
      - name: date
        description: "Date of the match"
      - name: stage
        description: "Stage of the competition"
      - name: format
        description: "Match format (e.g., BO3, BO5)"
      - name: team1
        description: "Identifier of team 1"
        tests:
          - not_null
      - name: team2
        description: "Identifier of team 2"
        tests:
          - not_null
      - name: score
        description: "Final match score"
      - name: maps_played
        description: "Number of maps won per side"
      - name: patch
        description: "Game patch used"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "match_id"]

  - name: base_matches_paris_2025
    description: "Clean base model for matches_paris. Adds a fixed event_id and applies minimal column cleaning."
    config:
      tags: ["base", "matches", "paris"]
    columns:
      - name: match_id
        description: "Match identifier local to this specific tournament"
        tests:
          - not_null
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_paris_2025"]
      - name: match_title
        description: "Raw match title"
        tests:
          - not_null
      - name: date
        description: "Date of the match"
      - name: stage
        description: "Stage of the competition"
      - name: format
        description: "Match format (e.g., BO3, BO5)"
      - name: team1
        description: "Identifier of team 1"
        tests:
          - not_null
      - name: team2
        description: "Identifier of team 2"
        tests:
          - not_null
      - name: score
        description: "Final match score"
      - name: maps_played
        description: "Number of maps won per side"
      - name: patch
        description: "Game patch used"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "match_id"]

  - name: base_matches_toronto_2025
    description: "Clean base model for matches_toronto. Adds a fixed event_id and applies minimal column cleaning."
    config:
      tags: ["base", "matches", "toronto"]
    columns:
      - name: match_id
        description: "Match identifier local to this specific tournament"
        tests:
          - not_null
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_toronto_2025"]
      - name: match_title
        description: "Raw match title"
        tests:
          - not_null
      - name: date
        description: "Date of the match"
      - name: stage
        description: "Stage of the competition"
      - name: format
        description: "Match format (e.g., BO3, BO5)"
      - name: team1
        description: "Identifier of team 1"
        tests:
          - not_null
      - name: team2
        description: "Identifier of team 2"
        tests:
          - not_null
      - name: score
        description: "Final match score"
      - name: maps_played
        description: "Number of maps won per side"
      - name: patch
        description: "Game patch used"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "match_id"]

  - name: stg_matches
    description: >
      Union of all base match tables across tournaments.
      Provides a global surrogate key match_pk and standardized match attributes.
    config:
      tags: ["staging", "bridge", "matches"]
    columns:
      - name: match_pk
        description: "Global surrogate match key"
        tests:
          - not_null
          - unique
      - name: event_id
        description: "Tournament event ID"
        tests:
          - not_null
      - name: match_title
        description: "Raw match title"
        tests:
          - not_null
      - name: date
        description: "Match date"
      - name: stage
        description: "Stage of competition"
      - name: format
        description: "Match format"
      - name: team1
        description: "Team 1 local identifier"
        tests:
          - not_null
      - name: team2
        description: "Team 2 local identifier"
        tests:
          - not_null
      - name: score
        description: "Final match score"
      - name: maps_played
        description: "Maps played during the match"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "match_title"]

  - name: base_map_stats_bangkok_2025
    description: "Clean base model for map_stats_bangkok. Adds a fixed event_id and standardized typing."
    config:
      tags: ["base", "map_stats", "bangkok"]
    columns:
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_bangkok_2025"]
      - name: map_id
        description: "Map identifier inside the event"
        tests:
          - not_null
      - name: times_played
        description: "How many times the map was played"
        tests:
          - not_null
      - name: attack_win_percent
        description: "Percent of attack-sided wins"
      - name: defense_win_percent
        description: "Percent of defense-sided wins"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "map_id"]

  - name: base_map_stats_paris_2025
    description: "Clean base model for map_stats_paris. Adds a fixed event_id and standardized typing."
    config:
      tags: ["base", "map_stats", "paris"]
    columns:
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_paris_2025"]
      - name: map_id
        description: "Map identifier inside the event"
        tests:
          - not_null
      - name: times_played
        description: "How many times the map was played"
        tests:
          - not_null
      - name: attack_win_percent
        description: "Percent of attack-sided wins"
      - name: defense_win_percent
        description: "Percent of defense-sided wins"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "map_id"]

  - name: base_map_stats_toronto_2025
    description: "Clean base model for map_stats_toronto. Adds a fixed event_id and standardized typing."
    config:
      tags: ["base", "map_stats", "toronto"]
    columns:
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_toronto_2025"]
      - name: map_id
        description: "Map identifier inside the event"
        tests:
          - not_null
      - name: times_played
        description: "How many times the map was played"
        tests:
          - not_null
      - name: attack_win_percent
        description: "Percent of attack-sided wins"
      - name: defense_win_percent
        description: "Percent of defense-sided wins"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "map_id"]

  - name: stg_map_stats
    description: >
      Union of all tournament-level map stat tables.  
      Provides a standardized set of map performance metrics per map per event.
    config:
      tags: ["staging", "bridge", "map_stats"]
    columns:
      - name: event_id
        description: "Tournament event ID"
        tests:
          - not_null
      - name: map_id
        description: "Map identifier inside the event"
        tests:
          - not_null
      - name: times_played
        description: "How many times the map was played"
      - name: attack_win_percent
        description: "Percent of attack-sided victories"
      - name: defense_win_percent
        description: "Percent of defense-sided victories"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "map_id"]

  - name: base_agents_stats_bangkok_2025
    description: "Clean base model for agent_stats_bangkok. Adds minimal typing and event ID validation."
    config:
      tags: ["base", "agent_stats", "bangkok"]
    columns:
      - name: agent_id
        description: "Agent identifier local to the stats table"
        tests:
          - not_null
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_bangkok_2025"]
      - name: utilization_percent
        description: "Agent utilization percentage across all matches"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "agent_id"]

  - name: base_agents_stats_paris_2025
    description: "Clean base model for agent_stats_paris. Adds minimal typing and event ID validation."
    config:
      tags: ["base", "agent_stats", "paris"]
    columns:
      - name: agent_id
        description: "Agent identifier local to the stats table"
        tests:
          - not_null
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_paris_2025"]
      - name: utilization_percent
        description: "Agent utilization percentage across all matches"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "agent_id"]

  - name: base_agents_stats_toronto_2025
    description: "Clean base model for agent_stats_toronto. Adds minimal typing and event ID validation."
    config:
      tags: ["base", "agent_stats", "toronto"]
    columns:
      - name: agent_id
        description: "Agent identifier local to the stats table"
        tests:
          - not_null
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_toronto_2025"]
      - name: utilization_percent
        description: "Agent utilization percentage across all matches"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "agent_id"]

  - name: stg_agent_stats
    description: >
      Union of all agent stats across tournaments.  
      Provides normalized agent usage percentages with standard field names.
    config:
      tags: ["staging", "agent_stats", "bridge"]
    columns:
      - name: agent_id
        description: "Agent ID (local numeric identifier)"
        tests:
          - not_null
      - name: event_id
        description: "Tournament identifier"
        tests:
          - not_null
      - name: utilization_percent
        description: "Final agent usage percentage"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "agent_id"]

  - name: base_players_bangkok_2025
    description: "Clean base model for players_bangkok. Adds a fixed event_id and minimal column cleaning."
    config:
      tags: ["base", "players", "bangkok"]
    columns:
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_bangkok_2025"]
      - name: player_id
        description: "Player identifier local to this specific tournament"
        tests:
          - not_null
      - name: player_name
        description: "Raw player name"
        tests:
          - not_null
      - name: team_id
        description: "Local team identifier for the player"
        tests:
          - not_null
      - name: start_date
        description: "Start date of player's roster period"
      - name: end_date
        description: "End date of player's roster period"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "player_id"]

  - name: base_players_paris_2025
    description: "Clean base model for players_paris. Adds a fixed event_id and minimal column cleaning."
    config:
      tags: ["base", "players", "paris"]
    columns:
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_paris_2025"]
      - name: player_id
        description: "Player identifier local to this specific tournament"
        tests:
          - not_null
      - name: player_name
        description: "Raw player name"
        tests:
          - not_null
      - name: team_id
        description: "Local team identifier for the player"
        tests:
          - not_null
      - name: start_date
        description: "Start date of player's roster period"
      - name: end_date
        description: "End date of player's roster period"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "player_id"]

  - name: base_players_toronto_2025
    description: "Clean base model for players_toronto. Adds a fixed event_id and minimal column cleaning."
    config:
      tags: ["base", "players", "toronto"]
    columns:
      - name: event_id
        description: "Logical identifier for the event"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_toronto_2025"]
      - name: player_id
        description: "Player identifier local to this specific tournament"
        tests:
          - not_null
      - name: player_name
        description: "Raw player name"
        tests:
          - not_null
      - name: team_id
        description: "Local team identifier for the player"
        tests:
          - not_null
      - name: start_date
        description: "Start date of player's roster period"
      - name: end_date
        description: "End date of player's roster period"

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "player_id"]

  - name: stg_players
    description: >
      Union of all base player tables across tournaments.  
      Generates a normalized player name and a surrogate key player_pk for cross-tournament analysis.
    config:
      tags: ["staging", "bridge", "players"]
    columns:
      - name: player_pk
        description: "Global surrogate player key"
        tests:
          - not_null
          - unique
      - name: event_id
        description: "Tournament event ID"
        tests:
          - not_null
      - name: player_id
        description: "Player ID local to the event"
        tests:
          - not_null
      - name: player_name
        description: "Raw player name"
        tests:
          - not_null
      - name: player_name_norm
        description: "Normalized player name (lowercase, trimmed)"
        tests:
          - not_null
      - name: team_id
        description: "Team ID local to the event"
        tests:
          - not_null
      - name: start_date
        description: "Start date of player's roster period"
      - name: end_date
        description: "End date of player's roster period"

  - name: stg_event_info
    description: >
      Staging table for tournament events.  
      Normalizes the event_id as a surrogate key (hashed) and keeps all relevant attributes for cross-tournament analysis.
    config:
      tags: ["staging", "events"]
    columns:
      - name: event_id
        description: "Hashed surrogate key for the event"
        tests:
          - not_null
          - unique
      - name: event_description
        description: "Original event identifier from raw source"
        tests:
          - not_null
      - name: event_name
        description: "Official name of the event"
        tests:
          - not_null
      - name: url
        description: "URL with event information"
      - name: title
        description: "Event title"
      - name: subtitle
        description: "Event subtitle"
      - name: start_date
        description: "Event start date"
      - name: end_date
        description: "Event end date"
      - name: price_pool
        description: "Prize pool for the event"
      - name: location
        description: "Event location"

  - name: base_detailed_match_bangkok_2025
    description: "Clean base model for map_matches_bangkok. Adds a fixed event_id and minimal column cleaning."
    config:
      tags: ["base", "map_matches", "bangkok"]
    columns:
      - name: event_id
        description: "Logical identifier for the event (e.g., masters_bangkok_2025)"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_bangkok_2025"]
      - name: map_match_id
        description: "Unique ID for the map-match row within this table"
        tests:
          - not_null
      - name: match_id
        description: "Match ID local to the tournament"
        tests:
          - not_null
      - name: map_id
        description: "Map ID local to the tournament"
        tests:
          - not_null
      - name: map_order
        description: "Order of the map in the match (1, 2, 3 …)"
        tests:
          - not_null
      - name: score
        description: "Score of the map (e.g., '13 - 9')"
      - name: winner_id
        description: "ID of the winner of the map"
      - name: duration
        description: "Duration of the map"
      - name: picked_by
        description: "ID of the team/player who picked the map"
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "map_match_id"]

  - name: base_detailed_match_paris_2025
    description: "Clean base model for map_matches_paris. Adds a fixed event_id and minimal column cleaning."
    config:
      tags: ["base", "map_matches", "paris"]
    columns:
      - name: event_id
        description: "Logical identifier for the event (e.g., masters_paris_2025)"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_paris_2025"]
      - name: map_match_id
        description: "Unique ID for the map-match row within this table"
        tests:
          - not_null
      - name: match_id
        description: "Match ID local to the tournament"
        tests:
          - not_null
      - name: map_id
        description: "Map ID local to the tournament"
        tests:
          - not_null
      - name: map_order
        description: "Order of the map in the match"
        tests:
          - not_null
      - name: score
        description: "Score of the map"
      - name: winner_id
        description: "ID of the winner of the map"
      - name: duration
        description: "Duration of the map"
      - name: picked_by
        description: "ID of the team/player who picked the map"
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "map_match_id"]

  - name: base_detailed_match_toronto_2025
    description: "Clean base model for map_matches_toronto. Adds a fixed event_id and minimal column cleaning."
    config:
      tags: ["base", "map_matches", "toronto"]
    columns:
      - name: event_id
        description: "Logical identifier for the event (e.g., masters_toronto_2025)"
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["masters_toronto_2025"]
      - name: map_match_id
        description: "Unique ID for the map-match row within this table"
        tests:
          - not_null
      - name: match_id
        description: "Match ID local to the tournament"
        tests:
          - not_null
      - name: map_id
        description: "Map ID local to the tournament"
        tests:
          - not_null
      - name: map_order
        description: "Order of the map in the match"
        tests:
          - not_null
      - name: score
        description: "Score of the map"
      - name: winner_id
        description: "ID of the winner of the map"
      - name: duration
        description: "Duration of the map"
      - name: picked_by
        description: "ID of the team/player who picked the map"
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "map_match_id"]

  - name: stg_detailed_match_map
    description: >
      Union of all map-match tables across tournaments.
      Provides a global surrogate key (map_match_pk) and keeps match-map-level stats.
    config:
      tags: ["staging", "bridge", "map_matches"]
    columns:
      - name: map_match_pk
        description: "Global surrogate key for the map-match relationship"
        tests:
          - not_null
          - unique
      - name: event_id
        description: "Tournament event ID"
        tests:
          - not_null
      - name: match_id
        description: "Match ID local to the tournament"
        tests:
          - not_null
      - name: map_id
        description: "Map ID local to the tournament"
        tests:
          - not_null
      - name: map_order
        description: "Order of the map within the match (1, 2, 3 …)"
        tests:
          - not_null
      - name: score
        description: "Score of the map (e.g., '13 - 9')"
      - name: winner_id
        description: "ID of the team/player that won this map"
      - name: duration
        description: "Duration of the map in seconds or minutes"
      - name: picked_by
        description: "ID of the team/player who picked the map"
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: ["event_id", "match_id", "map_id", "map_order"]
